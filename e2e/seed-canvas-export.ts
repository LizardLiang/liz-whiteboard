// Dedicated entry point for canvas-export.spec.ts. The representative search
// board already contains shapes, text, and a connector, so the canonical
// canvas seed remains the single source of fixture geometry.
import './seed-canvas'

console.log('[e2e seed-canvas-export] representative canvas export board ready')
