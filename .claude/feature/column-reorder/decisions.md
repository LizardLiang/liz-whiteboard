# Decisions Log -- Column Reorder

## Product Decisions (Athena -- PRD Creation)

| Decision                                                                                                | Rationale                                                                                                                                                                                                                                                                                                                                            | Rejected Alternative                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Drag handle on the left edge of every column row (always visible, not hover-only)                       | Reduces discovery friction — a user opening the table for the first time sees the affordance immediately. The same project-wide pattern was applied to the "+" Add Column button (per `dynamic-field-management` decisions). Hover-only handles are a known usability problem.                                                                       | Hover-only handle: less visual noise but worse discoverability. Drag-anywhere-on-row: ambiguous with single-click selection and double-click-to-edit which already exist on the row.                                                                                                  |
| Drag handle is the ONLY initiator of a reorder drag                                                     | The column row already supports double-click to edit, single-click to toggle constraint badges, and React Flow node selection. Adding "drag from anywhere on the row" would conflict with all of these. Restricting the drag-initiator to the handle keeps every existing interaction working without modification.                                | Drag-anywhere: simpler to discover but breaks existing inline-edit interactions. Modifier-key drag (e.g., Cmd+drag): power-user-only, undiscoverable.                                                                                                                                |
| Insertion line + ghost row TOGETHER (not just one)                                                      | User explicitly requested both in clarification. The two together address two questions the user asks during the drag: (1) "what am I dragging?" (ghost row) and (2) "where will it land?" (insertion line). Either alone leaves one of those questions unanswered.                                                                                  | Ghost only: user has to guess the drop slot. Insertion line only: user loses sight of which column they grabbed when the cursor is far from the source row.                                                                                                                          |
| Last-write-wins for concurrent reorder                                                                  | User explicitly affirmed in clarification. Matches the existing `dynamic-field-management` decision for column edits (FM-05 in that PRD, decision row "Last-write-wins for concurrent edits"). Keeping the same conflict policy app-wide reduces user confusion. **Revision 1: this decision is now contingent on REQ-14's overwrite-notification — silent last-write-wins is unacceptable for a batch operation.** | Operational Transform / CRDT: significantly more complex, no user request. Optimistic locking with version field: would force a conflict-resolution UI which the user did not ask for.                                                                                               |
| New `column:reorder` / `column:reordered` WebSocket events (NOT a sequence of `column:update` events)   | A reorder is one user action that affects N columns atomically. Broadcasting N separate `column:update` events would cause flicker, transient invalid states (two columns at order=3), and conflate the "value of `order` changed" channel with the "user reordered" channel. A dedicated event lets receivers apply the new order in one render pass. | Reuse `column:update`: would force receivers to render N intermediate states. Reuse `sync:request`: forces a full whiteboard re-fetch for every reorder, wasteful.                                                                                                                  |
| Reorder broadcast carries the FULL ordered ID list (not delta)                                          | Receivers can replace their order array directly with no risk of off-by-one or missing-update drift. The size cost is negligible (UUIDs × column count, typically a few hundred bytes). Robust against out-of-order delivery — the latest event always represents the full truth.                                                                   | `{ columnId, oldOrder, newOrder }` deltas: smaller payload but receivers must reconstruct the array, and a missed delta breaks the sequence permanently until next sync.                                                                                                            |
| Cross-table column drag is OUT of V1                                                                    | User explicitly affirmed in clarification. Cross-table drag changes the column's `tableId`, which has cascading consequences for relationships (FK references, edge endpoints) that are an entirely different feature.                                                                                                                              | Allow cross-table drag: significantly larger scope, raises questions about what happens to relationships when a column moves to a different table.                                                                                                                                  |
| Undo/redo is OUT of V1                                                                                  | User explicitly affirmed in clarification. The app has no undo/redo infrastructure today (the `dynamic-field-management` PRD also excluded it). Adding undo for one operation in isolation would be inconsistent with the rest of the app and require an undo stack architecture.                                                                  | Reorder-specific undo (e.g., a brief "Undo" toast): half-measure, inconsistent with rest of app.                                                                                                                                                                                     |
| Touch / mobile drag is OUT of V1                                                                        | The whiteboard is a desktop tool. The `dynamic-field-management` PRD applied the same exclusion. Touch DnD requires different gesture handling (long-press to initiate, larger hit targets) and a separate UX pass.                                                                                                                                  | Include touch in V1: doubles the test surface and the gesture conflict surface (touch + React Flow's existing pinch-to-zoom).                                                                                                                                                       |
| Keyboard reorder (Alt+Up/Down) is P2 STRETCH (not in V1 P0/P1) — **with explicit WCAG 2.1.1 Level A debt logged in Section 12 of PRD** | User clarification gave Athena the call. Reasoning: column rows are not currently keyboard-focusable in the existing `ColumnRow` component (verified — no `tabindex` in the current code). Making column rows keyboard-focusable + adding the Alt+Arrow handlers + integrating with the same reorder backend is meaningful work that should not block the core mouse-DnD shipping. P2 with explicit "must not block ship" clause preserves the option without risk. **Revision 1: the WCAG 2.1.1 Level A non-conformance is explicit, contingent on no contractual/regulatory obligation conflicting with it. Tech-spec must verify before V1 ship.** | Include in V1 P0: blocks ship on focus-management work that has nothing to do with the core feature. Exclude entirely: loses an accessibility win that's well-aligned with the feature.                                                                                              |
| Auto-scroll during drag is P1 (should-have, not must-have)                                              | Most table nodes in this app fit fully on screen. Auto-scroll only matters for tables that overflow the viewport. Treating it as P1 means it ships if implementation cost is low and is acceptable to defer if it isn't. Aligns with the "should-have" tier in `dynamic-field-management` PRD.                                                       | P0: would force shipping a polish feature on the critical path. Out of scope: degrades UX on tall tables.                                                                                                                                                                            |
| Escape-to-cancel during drag is P0                                                                      | A user who starts a drag and changes their mind needs an unambiguous cancel. Without Escape, the only way to abort is to drop in the original slot, which depends on cursor accuracy and feels accidental. Escape is the conventional cancel key in every desktop tool. Implementation cost is low.                                                  | No cancel: user must drop somewhere. Click-elsewhere-to-cancel: ambiguous with React Flow's pan/select.                                                                                                                                                                              |
| No-op drop (drop in original slot) emits ZERO writes and ZERO broadcasts                                | User explicitly required in clarification. Critical for two reasons: (1) avoids polluting the audit log / WebSocket traffic with non-changes, (2) prevents triggering collaborator re-renders when nothing has actually changed. The check is "did the ordered ID array change?" — a cleaner contract than "did the cursor move?".               | Always write: simpler code path but generates noise. Always broadcast: causes other clients to re-render for no reason.                                                                                                                                                              |
| Edges re-anchor automatically (no manual edge-update logic in this PRD)                                 | The existing React Flow + column-level handles architecture already attaches edges to column rows by handle ID. When a column row's DOM position changes, React Flow's edge anchor follows. This PRD requires the BEHAVIOR (AC-05a) but does not prescribe HOW — that's a tech-spec concern. Assumption A6 captures the risk that this may need explicit `updateNodeInternals` calls. | Specify the React Flow API to use: would be a tech-spec decision encroaching on Hephaestus's domain.                                                                                                                                                                                 |
| Permissions: any collaborator can reorder (no new RBAC gate)                                            | User explicitly affirmed in clarification: "any whiteboard collaborator has full edit rights". Matches existing column-edit permission model. The `updateColumnOrderFn` HTTP function has commented-out RBAC scaffolding (lines 300-308 in `columns.ts`), which the team has chosen to keep disabled. This PRD does not introduce a new policy.   | Add EDITOR-role gate: contradicts the user's stated default and would be inconsistent with existing column CRUD.                                                                                                                                                                     |
| IDOR check on reorder: server validates `tableId` belongs to caller's whiteboard AND every `orderedColumnIds` entry belongs to `tableId` | The existing `column:update` handler already does the table-ownership check (`collaboration.ts:625-644`). For reorder, an additional check is needed: the orderedColumnIds list must not reference columns from other tables (which could be a sneaky way to manipulate columns the caller shouldn't touch). This belongs in the PRD as a security requirement (AC-04f), not buried in tech-spec. | Skip the per-ID validation: trusts client input. Rejected for defense-in-depth.                                                                                                                                                                                                      |
| Optimistic UI is P1, not P0                                                                             | Same rationale as `dynamic-field-management` decision: core functionality (persist + sync) must work first; perceived-performance is a polish layer that can be added without blocking the feature. The drag itself already provides instant local feedback (the column row visibly moves on drop) even before optimistic update is wired in.   | P0: would block ship on a UX-polish feature.                                                                                                                                                                                                                                          |
| Drag-and-drop library: choice deferred to tech-spec (Hephaestus)                                        | Multiple viable options exist (`@dnd-kit/core` + `@dnd-kit/sortable` is the modern default; `react-dnd` is more mature; a custom pointer-event implementation is possible). The choice involves React Flow compatibility evaluation that is a technical concern, not a product concern. PRD records this as Open Question OQ-2-equivalent. **Revision 1: choice is now gated on Spike S1 (Section 13 of PRD).** | Mandate `@dnd-kit` in PRD: encroaches on tech-spec domain. The user's CLAUDE.md project rules permit DnD libraries (the rule is "shadcn + Tailwind for UI"; DnD is a behavior library).                                                                                              |

## Product Decisions (Athena -- Revision 1, addressing Nemesis review 2026-04-30)

| Decision                                                                                                | Rationale                                                                                                                                                                                                                                                                                                                                            | Rejected Alternative                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **REQ-14 — In-flight overwrite notification is P0** (resolves BLOCKING #1)                              | Silent overwrite of a teammate's reorder is textbook collaborative-data-loss UX. A passive toast preserves last-write-wins (no policy change) while ensuring the originating user knows their drop overrode someone else's change. The toast is dismissable; auto-dismiss is 8s (longer than standard) since the message is informational and the user may need to read both clauses. | Hard-block A's reorder ("rebase or cancel" prompt): more disruptive, breaks the feel of direct manipulation. Discard A's reorder when buffered remote arrives: user-hostile in the opposite direction (now A's work is silently lost). Apply remote during drag: yanks rows out from under cursor — disorienting. |
| **REQ-08 AC-08e — Reconnect sync mismatch surfaces a toast** (resolves BLOCKING #2)                     | Without this, a user who drops while disconnected sees their optimistic order, then sees it silently revert on reconnect-sync. The user has no signal anything went wrong. The toast tells them their last reorder may not have saved, with guidance to verify and retry. The detection is purely "after sync, does server order differ from my optimistic order?" — no special tagging needed. | Persist optimistic reorders in localStorage and replay on reconnect: heavier, may replay stale intents. Block drops during disconnect: more user-hostile, blocks legitimate work.                                                                                                  |
| **p95 latency methodology — measured via `performance.mark` from `pointerup` to rAF DOM-confirm; sample ≥ 30; warm cache; localhost AND LAN targets** (resolves BLOCKING #3) | Localhost-only targets are unverifiable for the user's actual environment (per `feedback_secure_context.md`, the user develops over LAN). Adding a LAN target (1000ms p95) acknowledges real-world conditions while keeping the localhost target for unit-style perf tests. The methodology specifies start (`pointerup` timestamp), end (rAF callback observing new DOM order), and sample size. | Localhost-only target: ships unverifiable performance claims. No methodology: untestable goal.                                                                                                                                                                                       |
| **Optimistic <100ms methodology — `pointerup` event timestamp to next-rAF-with-new-DOM-order; `performance.mark` instrumentation** (resolves BLOCKING #4) | "Drop" was ambiguous — it could mean `pointerup`, `dragend`, or post-no-op-check. Pinning it to the `pointerup` DOM event timestamp is unambiguous and matches what users perceive as "the moment I let go". Measurement via `performance.mark` is built into browsers and adds no test-harness complexity. | "Drop" without definition: unverifiable. Use `dragend`: fires later than `pointerup` and could mask local lag.                                                                                                                                                                       |
| **REQ-12 — Drag handle tooltip ("Drag to reorder") is the primary discovery vector** (resolves BLOCKING #5) | Visibility of the handle does not equal discoverability of the affordance. Existing users have a mental model of column rows that does not include a handle. A 400ms-hover tooltip with the text "Drag to reorder" gives both new and returning users an unambiguous textual cue. Tooltip uses existing shadcn/ui component — no new UI infrastructure. | Release-notes-only discovery: user must read release notes (low-engagement vector). Onboarding tour: heavyweight for one feature. Persistent banner: visual noise for users who already know.                                                                                       |
| **REQ-03 transactional batch update is MANDATORY** (resolves MAJOR — partial transaction success)        | The Nemesis review correctly noted that the previous PRD allowed "Option B" — N parallel per-column updates — which cannot satisfy AC-03b (atomic on failure) without compensating logic. Mandating a single Prisma transaction makes atomicity trivially correct and removes a foot-gun from tech-spec. The frontend's batch endpoint (whether HTTP or WebSocket) MUST wrap updates in `prisma.$transaction`. | Allow Option B with compensating undo: more complex, more failure modes. Allow Option B without compensation: violates AC-03b.                                                                                                                                                       |
| **WCAG 2.1.1 Level A non-conformance is an explicitly-accepted V1 debt** (resolves MAJOR — accessibility) | Promoting REQ-11 to P0 would block the core mouse-DnD ship on focus-management work. Explicitly logging the debt (Section 12 of PRD) makes the trade-off visible to the team, requires legal/contractual verification before ship, and creates a fast-follow ticket. The mitigation in V1 (aria-label + tooltip-aria-describedby) gives screen-reader users a textual cue about the affordance even though they cannot use it. | Promote REQ-11 to P0: blocks ship. Bury the WCAG failure in implementation notes: not visible enough for a Level A failure.                                                                                                                                                          |
| **FM-07 missing-column merge: append by ascending existing `order`, then re-sequence to 0..N-1** (resolves MAJOR — hand-wavy spec) | The previous "leaves missing columns at their existing `order` value (typically the largest)" wording relied on an unstated assumption that new columns are always appended with the largest order. This is true by current convention but is not enforced — a future code path could insert columns at the top. The deterministic rule (append-by-existing-ascending then re-sequence) does not depend on convention and produces the same observable behavior. | Reject A's reorder with VALIDATION_FAILED on missing-columns case: forces the client to refetch, more disruptive, increases failure mode count. Trust convention: brittle.                                                                                                          |
| **Personas explicitly enumerated with V1 in/out decisions** (resolves MAJOR — missing personas)         | Returning User, First-Time User, Mac trackpad, Reduced-motion, Screen-reader, Touch — each gets a yes/no/partially decision with justification. This makes the V1 scope auditable against real-user diversity rather than implicitly assumed. | Implicit personas: untraceable scope decisions. Single "developer" persona: erases the variety of users who actually use the whiteboard.                                                                                                                                            |
| **AC-02c offset = 8px right + 8px down (exact); AC-02d midpoint-snap with hysteresis on exact midpoint** (resolves MAJOR — untestable AC) | "Offset" was unmeasurable; "crosses midpoint" left edge cases. The exact pixel offset (8/8) matches @dnd-kit's default and is testable via DOM inspection. The midpoint-snap rule with hysteresis (line stays put on exact midpoint) prevents flicker at the boundary. | Offset by library default: not portable across library choices. Without hysteresis: insertion line could oscillate at exact midpoint.                                                                                                                                              |
| **REQ-15 — Toasts in this feature MUST NOT recommend "refresh" for operation-failed cases** (resolves MAJOR — refresh guidance) | Refresh discards the user's other in-flight optimistic edits in the whiteboard (which could include unrelated column edits, table renames). Telling the user to refresh after a transient reorder failure could destroy unrelated unsaved work. "Try again" is the correct guidance. The connection-status indicator covers the "you may need to refresh" case for connection-degraded scenarios. | Allow "refresh" guidance: risks destroying user's other unsaved work. No guidance text: leaves user without a clear next step.                                                                                                                                                       |
| **Assumption A4 elevated to a required pre-tech-spec spike (S1)** (resolves MAJOR — weak A4 evidence)   | Inline-edit's pointer behavior (click on an input) is materially different from a sustained pointer-down with movement. The "precedent" was weak. Mandating a spike before tech-spec entry catches the React Flow incompatibility risk before Hephaestus commits to a library. The spike is small (30 minutes) and produces a yes/no architectural decision. | Trust the precedent: medium-to-high risk of tech-spec rework if A4 fails. Skip the spike, plan for a fallback: doubles the implementation surface.                                                                                                                                  |
| **Assumption A6 also gets a spike (S2)**                                                                | Same logic as A4 — verifying React Flow's edge-anchor behavior in a 15-minute proof-of-concept removes a "we'll find out at integration time" risk. The outcome (do we need `updateNodeInternals` and when?) directly informs the tech spec. | Defer to implementation: same risk profile as A4 deferral.                                                                                                                                                                                                                          |
| **REQ-09 auto-scroll velocity is 600 px/s (default) / 300 px/s (reduced-motion)** (resolves MAJOR — vague "moderate speed") | "Moderate speed" was unmeasurable. 600 px/s is in the range of @dnd-kit's defaults and feels controlled in similar tools. The reduced-motion variant (300 px/s, no easing) respects user preference without disabling auto-scroll entirely (which would degrade UX for tall tables). Tech-spec may use the chosen library's documented default if within 20% — that flexibility is permitted because the exact value is a tuning concern, not a product concern. | "Moderate" without a number: untestable. Library default only: ties PRD to a specific library.                                                                                                                                                                                       |
| **REQ-13 — Reduced-motion compliance for ghost row + insertion line** (new requirement)                 | Users with `prefers-reduced-motion: reduce` set have explicitly opted out of motion effects. Smoothing/easing on the ghost row and insertion line during drag would violate that preference. The check is performed once per drag start (cheap) and applies for the duration of that drag. | Smooth ghost-row tracking unconditionally: violates user preference. Disable drag for reduced-motion users: hostile — they still need to reorder.                                                                                                                                  |
| **AC-08d — pending-reorder queue is FIFO bounded to 5; over-limit shows toast and blocks drag**         | Rapid-succession reorders (5 in 2 seconds) are a real persona. Without a bound, the queue could grow indefinitely if the network is slow, leading to ordering inconsistencies. FIFO with a small bound preserves order while signalling to the user when they've outpaced the network. | Unbounded queue: risks ordering errors and memory bloat. Reject every reorder while one is pending: too restrictive for the common case.                                                                                                                                            |
| **`reorderedBy` field justified by REQ-14** (resolves MINOR — scope drift)                              | The Nemesis review correctly flagged that `reorderedBy` had no consumer in the original PRD. REQ-14 now uses it: the buffered remote event's `reorderedBy` may be included in the overwrite-notification toast as the collaborator's display name (when available). This justifies its presence in the payload. | Remove `reorderedBy`: would force REQ-14's toast to be generic ("another collaborator") rather than personal ("Alice"). Keeping it is cheap and informative.                                                                                                                       |
| **Performance threshold unified at 30 columns** (resolves MINOR — conflicting numbers)                  | The previous PRD had 40+ in A9 and 30 in OQ-5. Unified at 30 (the lower, more conservative number) for the tech-spec smoke-test threshold. | Keep both numbers: confusing. Pick 40+: less conservative, more risk of shipping with jank on real-user tables.                                                                                                                                                                       |

## Revision Requests

### Round 1 (Nemesis, 2026-04-30) — REVISIONS

Nemesis flagged 5 BLOCKING + 9 MAJOR + 6 MINOR issues. See `prd-challenge.md` for full findings. All BLOCKING + MAJOR items are addressed in PRD Revision 1; minor items addressed where they intersect with the major fixes. Detailed mapping is in PRD Appendix C: Revision History.

### Architecture Review (Apollo) — 2026-04-30

| Issue | Severity | Rationale | Required Change |
|-------|----------|-----------|-----------------|
| SA-H1: AC-08e/f detection misses first-ever-reorder loss | High | Spec compares server vs. `lastConfirmed`; if the user's first-ever reorder is lost on disconnect, `lastConfirmed` is undefined and the toast condition short-circuits to false. PRD AC-08f explicitly says detection must work "regardless of how the optimistic reorder was lost" — including when no prior ack exists. | Compare server-fetched order against `lastOptimisticByTable[tableId]` captured at every enqueue, gated on a `dirtyByTable: Set<string>` flag set on enqueue and cleared on next confirmed broadcast. The first-ever reorder loss case must produce the AC-08e toast. |
| SA-H2: REQ-14 overwrite logic violates AC-14e | High | Spec uses `arraysEqual(buffered, newOrderedIds)` and §6 R#4 acknowledges this is intentionally aggressive ("user-friendly false positives"). PRD AC-14e specifies suppress-toast condition is "buffered remote is a strict subset / no-op relative to A's local order" — broader than just exact equality. The current simplification is a known-wrong shortcut that contradicts the AC. | Implement AC-14e correctly: detect whether A's order overwrites at least one column-position B moved. Algorithm option: compare `movedByA` and `movedByB` sets relative to `preDragOrderRef`; toast iff their intersection is non-empty AND positions differ. |
| SA-H3: `applyServerOrder` overwrites in-flight optimistic state at queue depth ≥ 2 | High | When ack #1 arrives with FM-07-merged order, §2.4.3 calls `applyServerOrder` which rewrites `node.data.table.columns` directly. If emit #2 (a second optimistic reorder) is in flight, its optimistic state is wiped out. Causes user-visible snap-back, violating AC-04c ("between event receipt and next painted frame, no intermediate ordering observable") and AC-08a/b. | Make ack reconciliation queue-aware. Either: (a) splice only the FM-07 delta (new columns) into current optimistic state, preserving in-flight emit-#N's local moves; OR (b) skip `applyServerOrder` on ack when subsequent emits are pending and rely on the cumulative broadcast for emit #N to converge state. |
| SA-H4: AC-14f buffered-remote never applied on no-op drop | High | `handleDragEnd` returns early on no-op without flushing `bufferedRemoteByTable`. PRD AC-14f: "If A's drop is a no-op, the buffered remote order is then applied to A's view in the post-drop reconciliation." There is no such reconciliation effect defined in the spec. The remote user's reorder remains invisible to A indefinitely. | Define explicit post-drop reconciliation invoked on every drop. Steps: clear `localDraggingByTable[tableId]`; if buffer non-empty AND drop was no-op → call `applyServerOrder(tableId, buffered)`; else if buffer non-empty AND drop was non-no-op → run REQ-14 logic per SA-H2 then clear buffer. Also document where `setLocalDragging(tableId, true)` is called (`handleDragStart`). |
| SA-M1: `useEffect` does not guarantee AC-05d "same render pass" | Medium | `useEffect` runs post-paint by definition; AC-05d requires pre-paint. §6 R#1 already identifies this risk and proposes `useLayoutEffect` as the fix — but defers the decision to "measure first". The architecture should commit to `useLayoutEffect` upfront because it is the documented React API for the "no flicker between commit and paint" guarantee the AC requires. | Change §2.4.4's `useEffect` to `useLayoutEffect`. Move §6 R#1 from "open risk" to "resolved decision" in §5. |
| SA-M3: Queue-full check fires too late (drag-end, not drag-start) | Medium | The 6th drag's `pointerdown` is allowed to start, ghost row renders, user drags to a new position, then on drop the 6th's `reorderColumns` toasts and discards. User sees a phantom move that contradicts the "responsive" UX target. AC-08d's "shows a toast and does not initiate a new drag" implies blocking at drag-start. | Move the queue-full check (length ≥ 5) from `reorderColumns`/`handleDragEnd` into `handleDragStart`. If at cap, toast and `event.preventDefault()` (or @dnd-kit's drag-start cancellation API). |
| SA-L2: New WebSocket events not added to protocol docs | Low | The repo has `specs/001-collaborative-er-whiteboard/contracts/websocket-events.md` per project CLAUDE.md. The spec's §6 R#6 raises this but does not include the docs update in any phase. | Add to Phase 1 deliverables: "Update `specs/001-collaborative-er-whiteboard/contracts/websocket-events.md` to document `column:reorder`, `column:reordered`, and `column:reorder:ack` payloads." |

Full review at `.claude/feature/column-reorder/spec-review-sa.md`. Verdict: **Concerns**.

### Code Review (Hermes) — 2026-04-30

| Finding | Tier | Rationale | Required Fix |
|---------|------|-----------|--------------|
| B1: TableNode.new.tsx:246-322 — queue-full guard does not prevent persistence on drop | Tier 1 (Correct) + Tier 6 (Resilient) | `handleDragStart` early-returns when queue is full but `@dnd-kit` does not cancel the active drag; `handleDragEnd` then runs unconditionally and pushes a 6th entry past the FIFO cap, emits to server, and on error rollback uses an empty/stale `preDragOrderRef`. SA-M3's queue-full guard is bypassed in the drop path. | Add a guard at drag-end (`if (preDragOrderRef.current.length === 0) return`) and reset refs after every successful drop and cancel. Or programmatically cancel the active drag at drag-start. |
| B2: src/hooks/use-column-reorder-mutations.ts — three near-duplicate setNodes-reorder blocks | Tier 8 (Maintainable, M3) | `onColumnReorderedFromOther` (261-288), `applyServerOrder` (293-321), and `reconcileAfterDrop`'s real-drop path (367-385) duplicate the same colMap → filter → reorder → setNodes pattern. Future bug-fixes (e.g. preserving column-level metadata) must be made in three places. M3 with ≥2 copies is a BLOCKER. | Extract a private `applyOrderToNodes(tableId, orderedIds, setNodes)` helper and route all three call sites through it. No behavior change; existing tests pass. |

WARNINGs to address before approval (W1–W5 in code-review.md): dead `lastConfirmedOrderByTable` state (M1), `setNodes: () => {} as any` type leak (M4), stringly-typed error codes (M5), unbounded per-table refs across table-delete (M10), sequential awaits in socket handler (M6).

Full review at `.claude/feature/column-reorder/code-review.md`. Verdict: **Changes Required**.

### Resolution of Code Review Findings (Ares — Round 3, 2026-04-30)

All BLOCKER and WARNING items addressed:

| Finding | Resolution |
|---------|------------|
| B1: queue-full phantom drop | reconcileAfterDrop guards on `preDragOrder.length === 0` — phantom drops become no-ops, no state mutation, no server emit |
| B2: 3x setNodes duplication | Extracted `applyOrderToNodes(tableId, orderedIds, setNodes)` private helper; all 3 call sites delegate to it |
| HIGH-01: autoScroll canvas pan | `autoScroll={false}` added to DndContext |
| MEDIUM-04: console.log/trace | Removed both debug statements from edges-to-nodes effect |
| MEDIUM-01: onSyncReconcile unwired | `justReconnectedRef` flag added; initialNodes effect calls `onSyncReconcile` on reconnect refetch, closes AC-08e/f |
| W1: lastConfirmedOrderByTable dead state | **Kept as Map<string, Array<string>>** (not reduced to Set). Decision: the stored order array will be needed by MEDIUM-01 reconnect-stale-baseline refresh (Cassandra LOW-03). Reducing to Set now would require reverting when that path is wired. Rationale documented inline in hook. |
| W2: setNodes: () => {} as any | Pre-existing pattern; documented in implementation-notes as intentional workaround. The `handleColumnReorder` wrapper in ReactFlowWhiteboard always provides the real setNodes. The type signature now uses `SetNodes` with a JSDoc note. Full refactor to split DropEvent/ReconcileAfterDropParams deferred as tech debt (would break 3 test call sites). |
| W3: stringly-typed errors | Exported `ColumnReorderErrorCode` union type from use-column-reorder-mutations.ts; `onColumnReorderError` uses typed guard; `ColumnReorderErrorEvent` updated |
| W4: forgetTable not wired | `forgetTable()` exposed from hook; wired into `onTableDeleted` in ReactFlowWhiteboard |
| W5: sequential DB reads | Parallelised with `Promise.all([findDiagramTableById, findColumnsByTableId])` |

### Code Review Round 2 (Hermes) — 2026-04-30

| Finding | Tier | Rationale | Required Fix |
|---------|------|-----------|--------------|
| B1-A: TableNode.new.tsx:246-329 + use-column-reorder-mutations.ts:384 — queue-full guard at reconcileAfterDrop is unreachable in practice | Tier 1 (Correct) + Tier 6 (Resilient) | The `if (preDragOrder.length === 0) return` guard in `reconcileAfterDrop` only fires when `preDragOrderRef.current` is empty. The ref is initialized empty at mount and only ever WRITTEN (never reset between drags). After any successful drag, the ref holds a non-empty value indefinitely. A queue-full state can only exist after 5 successful drag-drops, so the ref is always populated when the guard is checked. Net effect: the original B1 bug (queue-full drop pushes a 6th entry past the cap, emits to server, corrupts state) is unfixed. | Reset `preDragOrderRef.current = []` and `preDragColumnsRef.current = []` at the start of `handleDragStart` BEFORE the `isQueueFullForTable` check, so a rejected drag-start clears stale state. Add a regression test that exercises queue-full + real `newOrder` (not the cancel path INT-36 already covers) through `reconcileAfterDrop` and asserts no setNodes/emit calls. |
| W4-A: use-table-mutations.ts:48-105 — forgetTable not called on local table deletion | Tier 8 (Maintainable, M10) | The Round 2 fix wired `forgetTable` into the `onTableDeleted` callback used by `useWhiteboardCollaboration` for the remote `table:deleted` socket event. Local deletes via `tableMutations.deleteTable` directly call `setNodes((prev) => prev.filter(...))` and bypass `onTableDeleted`, so the six per-table refs in `useColumnReorderMutations` (queueByTable, lastOptimisticByTable, lastConfirmedOrderByTable, dirtyByTable, bufferedRemoteByTable, localDraggingByTable) leak across local create/reorder/delete cycles. | Either add an `onLocalTableDeleted` callback parameter to `useTableMutations` and wire it to `columnReorderMutations.forgetTable`, or reroute the local-delete path through the existing `onTableDeleted` callback so both paths converge. |

Full review at `.claude/feature/column-reorder/code-review.md`. Verdict: **Changes Required**.

## Final Resolution

<!-- Athena will update this after all reviews are resolved -->

Pending re-review by Nemesis on Revision 1.

---

### PRD Alignment (Hera) — 2026-04-30

Verdict: **GAPS** — 54% coverage (27/50 ACs verified). Stage 8 returned to Ares.

| Criterion Group | Status | Gap |
|-----------------|--------|-----|
| AC-01a–AC-01g (REQ-01 Drag Handle) | gaps | S6 TableNode.test.tsx not created; all 7 ACs unverified |
| AC-02a–AC-02f (REQ-02 Drag Visual) | gaps | S6 not created; all 6 ACs unverified |
| AC-03a (REQ-03 Persist on reload) | gaps | S10 E2E not created; no automated persistence test |
| AC-04b (REQ-04 Broadcast scoped) | gaps | S5 socket handler not added to collaboration.test.ts |
| AC-04f (REQ-04 IDOR validation) | gaps | S5 not created |
| AC-05a–AC-05d (REQ-05 Edge Re-Anchor) | gaps | S8 ReactFlowWhiteboard.test.tsx not created |
| AC-07a–AC-07b (REQ-07 LWW convergence) | gaps | S5 not created; server-level broadcast untested |
| AC-07d (REQ-07 post-drop ack wins) | gaps | INT-25/26 not written (planned in S7 but absent) |
| AC-08d drag-start block (REQ-08 queue) | gaps | INT-19/20 not created (S6 deferred) |
| AC-09a–AC-09d (REQ-09 Auto-Scroll) | gaps | No auto-scroll tests in any created file |
| AC-10a–AC-10c (REQ-10 Escape Cancel) | gaps | S6 not created; INT-17/18 absent |
| AC-12a, AC-12e (REQ-12 Tooltip) | gaps | No tooltip tests in any created file |
| AC-13a–AC-13c (REQ-13 Reduced-Motion) | gaps | No reduced-motion tests in any created file |
